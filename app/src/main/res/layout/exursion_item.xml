<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>

        <variable
            name="excursion"
            type="com.example.moscowexcursion2.model.Data" />

        <variable
            name="onClick"
            type="com.example.moscowexcursion2.util.OnExcursionClick" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/clickExursion"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="@{onClick::onClick}"
        android:tag="@{excursion.title}">

        <ImageView
            android:id="@+id/exc_image"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:imageUrl="@{excursion.img}"
            android:scaleType="fitXY"
            android:visibility="visible"
            android:src="@mipmap/ic_launcher"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/exc_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="8dp"
            android:background="#afff"
            android:gravity="center"
            android:text="@{excursion.title}"
            android:visibility="visible"
            app:layout_constraintBottom_toBottomOf="@+id/exc_image"
            app:layout_constraintEnd_toEndOf="@+id/exc_image"
            app:layout_constraintStart_toStartOf="@+id/exc_image" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>